"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { defaultOrganizationId } from "../../lib/config";

export default function LoginPage() {
  const router = useRouter(); const [email, setEmail] = useState("auroriumnexus@gmail.com"); const [password, setPassword] = useState(""); const [org, setOrg] = useState(defaultOrganizationId); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(""); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, password, organizationId: org }) }); const body = await response.json().catch(() => ({})); if (!response.ok) setError(body.error ?? "Sign-in failed"); else router.push("/portfolio"); setBusy(false); }
  return <main className="login-shell"><section className="login-card"><div className="brand"><span className="brand-mark">✦</span> CoolProof</div><div className="eyebrow">Secure operations console</div><h1>Sign in to govern cooling investment.</h1><p>Sign in with AWS Cognito. CoolProof validates your organization membership with the backend before creating a session.</p><form className="login-form" onSubmit={submit}><div className="field"><label htmlFor="email">Work email</label><input id="email" name="email" autoComplete="username" type="email" value={email} onChange={e => setEmail(e.target.value)} required /></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" autoComplete="current-password" type="password" value={password} onChange={e => setPassword(e.target.value)} required /></div><div className="field"><label htmlFor="org">Organization ID</label><input id="org" name="organization" autoComplete="organization" value={org} onChange={e => setOrg(e.target.value)} required /><small className="muted">Your administrator provides this tenant ID.</small></div>{error && <div className="error" role="alert">{error}</div>}<button className="button primary" disabled={busy}>{busy ? "Authenticating and checking membership…" : "Sign in securely"}</button></form><div className="notice">AWS Cognito · backend membership validation · us-east-2</div></section></main>;
}
